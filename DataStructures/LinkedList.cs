namespace DataStructures
{
    // Singly linked list Generic
    public class Node<T>
    {
        public T nodeData;
        public Node<T> next;
        public Node(T data)
        {
            nodeData = data;
            next = null;
        }
    }
    public class LinkedList
    {
        
    }
}